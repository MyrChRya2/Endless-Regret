# 开发日志 2026-08-20

> 状态：收尾（管理侧工作，无引擎代码变更）

---

## 今日摘要

审查新增的两个 DSH 插件（meow-memory、dsh-file-explorer）的项目价值；卸载误装的 knowledge 包；`.dsh-meow/` 记忆数据加入 .gitignore。无引擎代码变更。

## 完成内容

- **插件审查**（DSH web profile 新增）：确认两个真插件——
  - `meow-memory`（记忆系统）：为本项目首次建立跨会话记忆库（overview/structure/decisions/todo/lessons/rules/topic 共 11 条）
  - `dsh-file-explorer`（GUI 文件浏览器）：查看文档/代码更直观
- **卸载误装包 `knowledge`**（v0.2.1）：以为是"本地知识库优化插件"，实为百度 EFE 2013 年前端展示站项目（非 DSH 插件），`dsh plugin --profile web remove knowledge` 已清除
- **`.dsh-meow/` 加入 `.gitignore`**：meow-memory 的记忆库数据（memory.db）不入版本控制
- **知识库用途澄清**（用户原话）："所有的检索于知识库都是给你们用的，我不需要"——记忆/知识库是 AI 侧资产，用户不做检索

## 决策与理由

- 卸载 knowledge → 误装（包名误导），项目知识库方案维持：meow-memory + docs/DEBUG_NOTES.md + 文档体系
- `.dsh-meow/` 忽略 → 与 content/ 同策略，AI 数据不进游戏仓库（吸取 8-17 误提交教训）
- 不引入额外知识库工具 → 用户明确不需要检索类工具，现状已满足

## 踩坑 / 问题

- 插件名误导：`knowledge` 听起来像知识库工具，实际是无关老项目——装插件前应先查包元数据（是否有 dsh 插件字段）

## 文件变更

- 修改：`.gitignore`（+`.dsh-meow/`）
- 环境变更：`~/.dsh/profiles/web/` 移除 knowledge 依赖（package.json + node_modules）

## 进度与下一步

- 当前里程碑 / 任务：M1 1.6 挂边（B1 待修复）
- 下一步（明日）：
  1. 修复 B1 挂边判定误判（重点）——疑 wall_escape 锁未复位 / 头顶瓦片误判
  2. 修复 B3（WallJump 贴另一面墙不进 WallGrab）
  3. B1 修复后同步 GDD 挂边状态名（ledge_hang → ledge_grab/ledge_slide）

## 阻塞 / 待决策

- B1/B3 待明日处理
- 知识库方向已定（AI 用，用户不需要检索工具）→ 无待决策

## 其他（素材 / 灵感 / 备忘）

- R1 首期视频素材：挂边 bug 修复过程是现成素材（OBS Replay Buffer 随手抓）
