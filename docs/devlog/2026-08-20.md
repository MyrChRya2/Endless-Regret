# 开发日志 2026-08-20

> 状态：收尾（M1 1.6 挂边系统功能完整：判定修复 + 状态机演进 + 动画占位体系）

---

## 今日摘要

插件审查与工具链收尾；**P0 B1 挂边判定误判修复**；挂边状态机演进（LedgeClimb 上爬状态：解耦 → 过程式 → 入口统一 jump）；动画占位体系（sprite_target 锚点占位 + error 兜底）；删除射线渲染调试；上爬手感微调。**M1 1.6 挂边功能完整**，剩动画帧。

## 完成内容

- **插件审查**（早晨）：确认 meow-memory / dsh-file-explorer 两个真插件；卸载误装包 knowledge；`.dsh-meow/` 加入 .gitignore
- **B1 挂边判定误判修复**（P0）：① 墙体内瓦片接缝误判为墙顶 → `is_head_touching_ledge` 增加空间判据（三判据）；② wall_escape 脱离锁未复位 → 离墙/超时自动复位。全图扫描 129 → 0 处误判
- **LedgeClimb 上爬状态**：从 LedgeGrab 解耦（顺带修复原上爬"只改 y 不改 x 爬上去悬空掉落"）→ 改为过程式（0.2s lerp 插值，配合动画）→ 入口统一 jump（移除 move_up 入口）
- **LedgeSlide 调整**：触墙类型 FULL 切回 WallGrab 慢滑；jump 进入 LedgeClimb（不再 WallJump）
- **动画占位体系**：`_handle_anim` 三分支——有帧正常播放 / 空帧（ledge_*/slide）播 `sprite_target` 锚点占位（不报错）/ 动画不存在播 `error` + push_error；注册 sprite_target（1 帧）
- **调试清理**：删除射线渲染调试代码（`_draw`/`_draw_ray`/`queue_redraw`）
- **上爬手感微调**（70ace00）：CLIMB_DURATION 0.2s、CLIMB_END_CLEARANCE 20、爬升终点经 Airborne 落定；error.png 占位更新
- **新任务登记**：P2 角色运动粒子特效（三方案思路，见补记 11）
- **导出与发布**：exports/ 目录配置（windows/web）、Release v0.0.3-alpha 先于 R1 首期视频决策

## 决策与理由

- B1 修复 → 接缝上方仍有瓦片（墙体延续）即非墙顶，空间判据拒绝；wall_escape 锁只该在"脱墙过渡"期间生效，离墙即复位
- LedgeClimb 独立状态（用户指令）→ 上爬与挂边解耦、便于动画扩展；jump 统一为上爬入口（用户确认"WallClimb"指 wall_grab）；LedgeSlide 完整贴墙即恢复 WallGrab 慢滑
- 过程式上爬 → 为 ledge_climb 动画铺路，时长占位（0.2s）待动画帧对齐
- 空帧动画播 sprite_target 而非 error → 开发期可视化 + 精灵中心锚点校准；仅"动画名不存在"才 push_error
- 删除射线渲染调试 → B1 后判定稳定，不再需要（方法保留在 DEBUG_NOTES）
- Release v0.0.3-alpha 先于 R1 首期视频 → 视频直接带可玩链接
- 以后辅助编程会话默认不 git 提交（用户："以后你不需要管提交，除非我特殊要求"）

## 踩坑 / 问题

- **墙体内瓦片接缝骗过高度+法线双判据**（DEBUG_NOTES 新增陷阱 6）：垂直射线偏移进墙内后命中相邻瓦片接缝，法线同为 (0,-1)——接缝与墙顶无法区分
- **GDScript 类型推断**：`var x := a - sign(b) * 16.0` 推断失败 → 状态脚本加载失败、`get_state` 返回 null、change_state 静默忽略（状态卡死）
- **headless 测试**：`Input.is_action_just_pressed` 边沿需要真实帧循环 + InputEventAction（手动驱动不刷新）
- **空帧动画**：`has_animation=true` 但 0 帧 → 不触发 error 兜底，玩家隐身
- **原上爬只改 y 不改 x** → 玩家挂在平台侧面时爬上去悬空掉落（探测确认）

## 文件变更

- 修改：`player.gd`（接缝空间判据、wall_escape 复位、`_handle_anim` 三分支、删渲染调试）、`ledge_grab.gd`、`ledge_slide.gd`、`player.tscn`（LedgeClimb 注册、sprite_target、动画重排、run 速度 20）、`error.png`、`docs/DEBUG_NOTES.md`（陷阱 6）、`docs/ROADMAP.md`（B1 ✅、1.6 勾选、决策记录）、`docs/GDD.md`（状态名同步）、`export_presets.cfg`
- 新增：`ledge_climb.gd`、`tools/` 回归套件（dump_tiles / test_b1 / test_b2 / test_head / test_climb）

## 进度与下一步

- 当前里程碑 / 任务：M1 1.6 挂边系统（功能完整，待动画帧）
- 下一步（明日）：
  1. 画挂边动画（Aseprite）：ledge_grab / ledge_slide / ledge_climb 三组左右向 → 填进 SpriteFrames 占位 → CLIMB_DURATION 对齐动画帧时长
  2. 画动画时录屏（OBS Replay Buffer）= R1 素材
  3. 修复 B3（WallJump 贴另一面墙不进 WallGrab）→ 手感验证 → M1 达标 → Release v0.0.3-alpha（先于 R1 首期视频）
  4. P2 角色运动粒子特效（M1 手感定型后启动，方案日后选定）

## 阻塞 / 待决策

- 挂边动画帧待画（Aseprite）
- 上爬手感（0.2s）待实机验证
- 粒子特效方案三选一（日后选定，见补记 11）

## 其他（素材 / 灵感 / 备忘）

- R1 首期视频素材：挂边 bug 修复过程（已发生）、挂边动画绘制录屏（待发生）
- exports/ 导出目录（windows/web）

---

## 补记（同日追加）

### project-management skill v2 发布
- **新增 `bug-track` 子技能**：bug 登记 → ROADMAP 跟踪表（ID/优先级/状态流转），路由表 + daily-wrap + doc-roles + 索引模板同步
- **新增 `README.en.md`**（英文版），与 README.md 顶部中英互切
- 推送 GitHub：`MyrChRya2/project-management`（commit `1da60b6`）
- 踩坑：HTTPS remote push 无凭据失败 → **remote 切换为 SSH**（`git@github.com`），后续推送无障碍
- 以后更新流程：AI 提醒 → 用户批准 → 修改 → 推 GitHub

### B1 调试起步（`Game_Build/tools/`）
- 新增调试工具目录：`tools/dump_tiles.gd`（瓦片 dump）、`tools/test_b1.gd`（B1 专项测试脚本）
- 首次 headless 扫描（`_b1_scan1.txt`）：**`test_b1.gd:30` 解析错误**——`_test_face_bug1()` 是协程（含 await），必须以 `await` 调用
- 待续：修复协程调用后重跑扫描，定位 B1 两场景

### 工作区说明
- `Game_Build/tools/`（2 个 gd）与 `_b1_scan1.txt`（调试输出）保持未提交，待 B1 修复后统一处理

---

## 补记 2（同日追加）

### B1 挂边判定误判修复（P0 重点）

**根因定位**（headless 自动扫描 + 定向复现，`tools/` 测试套件）

- **场景①（头顶有瓦片 → 误进 LedgeSlide/LedgeGrab）**：垂直平台顶射线偏移进墙体内部（表面内 2px）后，会命中**相邻瓦片之间的接缝边界**——接缝法线同样是 (0,-1) 的水平面，与真实墙顶无法区分。玩家沿墙下滑时，头顶每经过一个瓦片接缝就有约 8px 的误判窗口：WallGrab 中 `LEDGE_TOP → LedgeGrab` 错误转换，Airborne 中 `LEDGE_TOP → LedgeSlide` 错误进入。全图墙面扫描实测：墙列 0/75/98/113/120 均复现（修复前共 129 处标记）。
- **场景②（脱墙后不再回 WallGrab）**：`wall_escape` 脱离锁只在落地/跳跃时复位，WallGrab 脚底离墙进入 Airborne 后**整段下落都跳过贴墙检测**，再贴任何墙都不回 WallGrab。定向复现确认：锁置位时贴墙 300 帧不再抓墙，直接落地。

**修复**

- ① `is_head_touching_ledge()` 增加**空间判据**（与高度 + 法线组成三判据）：命中面正上方 2px 处用微型圆 `intersect_shape` 检测是否埋入碰撞体——接缝上方仍有瓦片（墙体延续）→ 拒绝；真实墙顶上方是空的 → 接受。
- ② 脱离锁自动复位：置位时记录 `wall_escape_time`（0.5s），`_physics_process` 中**完全离开墙面（is_on_wall=false）立即复位**，仍贴墙则超时兜底复位——覆盖"脱墙后落到下方/旁边墙面再贴"的路径。

**验证**（headless，`godot.windows.editor.x86_64.exe --headless --path Game_Build --script tools/xxx.gd`）

- `test_b1.gd`（全图墙面扫描）：修复前 129 处误判，修复后 0 处接缝误判；仅剩 -13 列 16 处为判定器误报（玩家滑过墙面底部后蹭到地板边缘的合法行为，修复前同样存在）
- `test_b2.gd`（脱离锁 3 场景）：修复前场景 2 正常 / 场景 3 不再抓墙（bug 复现）；修复后全过（空中复位 @0 帧、无锁贴墙即抓 @0 帧、带锁超时后抓 @31 帧）
- `test_head.gd`（判定函数单元测试）：合法墙顶 dy=-4/-8 仍判定为墙顶，墙体接缝 y=0 正确拒绝

**边界说明**

- 平台边缘的射线采样点恰好在紧邻结构正下方时（如柱压平台的角落），空间判据会拒绝挂该边缘——该处头顶净空确实受限，判定偏保守；权衡后保留（否则场景①无法修复）
- `_point_inside_world()` 原为调试工具，现同时供生产逻辑使用（已加注释）
- 扫描中 -13 列的 LedgeSlide↔Airborne 振荡是"滑过墙底后蹭地板边缘"的既有行为，非本次引入

**调试工具留存**：`Game_Build/tools/`（dump_tiles / test_b1 / test_b2 / test_head）作为 B1 回归测试套件随本次提交保留；`_b1_scan*.txt` 等调试输出不入库。

---

## 补记 3（同日追加）

### 导出与发布方案（决策）
- **`exports/` 导出目录**（项目根，不入库）：`exports/windows/`（.exe）+ `exports/web/`（html/wasm 配套）——平台分目录，版本靠文件名（`v0.0.3_alpha`）与 Git tag 隔离
- **`export_presets.cfg` 路径修正**：两个 preset 改相对路径（`../exports/windows|web/...`），还原绝对路径；顺手修正 Web preset 原路径的拼写错误（EndlessRegrat → EndlessRegret）
- **发布顺序（决策）**：**GitHub Release v0.0.3-alpha 先于 R1 首期视频**——视频发布时直接带可玩链接（已记入 ROADMAP R1 待决策区）
- 已推送：`8058b3c`（B1 修复）+ `60ad58f`（exports 配置 + tools/ uid 入库），main 与远程同步

### 下一步
1. 用户在 Godot 中导出 Windows + Web（路径已配好）→ 打包 zip
2. 打 tag `v0.0.3-alpha` → 创建 GitHub Release → 上传产物
3. 之后：B3 修复 → 手感验证 → M1 达标 → R1 首期视频（带 Release 链接）

---

## 补记 4（同日追加）

### LedgeClimb 上爬状态（解耦自 LedgeGrab）

**需求**（用户指令）：取消 LedgeGrab 状态点击 jump 进入 WallJump（蹬墙跳）的功能；把 LedgeGrab 的上爬解耦成独立 `LedgeClimb` 状态；LedgeGrab 点击 jump 进入该状态。

**改动**：
- 新增 `player/states/ledge_climb.gd`（WallState 子类）：承载原 `_climb_up()` 逻辑——头顶空间检测（点采样 6/14/22px × 碰撞盒宽 3 列）→ 爬上平台顶面 → 进 Idle；空间不足/失去墙面 → 退回 LedgeGrab 保持挂边
- `ledge_grab.gd`：删除 `_climb_up()`；jump 与 move_up 均改为进入 `Wall/LedgeClimb`（jump 不再走 WallJump）
- `player.tscn`：注册 `States/Wall/LedgeClimb` 节点 + `ledge_climb_left/right` 占位动画（空帧，与 ledge_grab 一致）

**顺带修正的既有 bug**：原 `_climb_up()` 只改 y 不改 x——玩家挂在平台侧面时爬上去会悬空掉落（headless 实测：爬升后 60 帧落到地板）。LedgeClimb 改为向平台内侧平移 16px（碰撞盒宽）站上平台顶面，挂左/右面均正确。

**验证**（`tools/test_climb.gd`，headless 真实帧循环 + InputEventAction）：4 场景全过——
1. 挂右面 + jump → LedgeClimb → Idle，站稳平台顶 (936, -416)
2. 挂右面 + move_up → 同上（上爬入口保留）
3. 平台顶上方加遮挡 + jump → LedgeClimb → 退回 LedgeGrab（不穿模）
4. 挂左面 + jump → LedgeClimb → Idle，站稳平台顶

**踩坑**：`var x := a - sign(b) * 16.0` 在 GDScript 静态分析下推断失败（Cannot infer）——显式标注 `var x: float`；该解析错误会让状态脚本失效、`get_state()` 返回 null、change_state 静默忽略（状态卡死），headless 测试直接暴露。

**遗留**：LedgeClimb 为瞬移式（一帧完成），爬升动画待 Aseprite 素材（`ledge_climb_*` 占位）；WallJump 仍保留在 WallGrab 的 jump 路径（未删状态）。

---

## 补记 5（同日追加）

### 挂边上爬入口调整（用户指令）

**需求**：① 移除 LedgeGrab 的 move_up 上爬入口（上爬只由 jump 触发）；② LedgeSlide 中触墙类型 FULL 时切回 WallGrab（用户表述"WallClimb"，经确认指 wall_grab 墙滑）；③ LedgeSlide 加入 LedgeClimb 入口（jump，与 LedgeGrab 统一——用户确认不再保留 LedgeSlide 的 WallJump）。

**改动**：
- `ledge_grab.gd`：仅保留 `jump → LedgeClimb`（删除 move_up 分支）
- `ledge_slide.gd`：`jump → LedgeClimb`（替代原 WallJump）；接触分类增加 **FULL → WallGrab**（完整贴墙切回慢滑，如从平台边缘滑落到旁边完整墙上）；LEDGE_TOP → LedgeGrab、NONE → Airborne、SLIDING 继续滑落
- `ledge_climb.gd`：失败回退按"头顶是否仍贴平台顶"分流——贴 → 退回 LedgeGrab（挂边）；不贴 → 退回 LedgeSlide（继续滑落）——支持从 LedgeSlide 进入后失败继续滑

**行为说明**：LedgeSlide 滑落中 jump = 直接扒上平台（平台顶在头顶 14px 射线范围内且净空足够时瞬移爬升，否则退回滑落）。

**无回归分析**：LedgeSlide→WallGrab 切换会重置 `_was_above`（挂边接近锁存），但该标志只在 dy>12（头顶高于墙顶 12px+）时置位，切换后 WallGrab 会重新累积（切换瞬间 dy 仍 >12 即置位）；dy≤12 时原 LedgeSlide 本就不挂边——FULL 切换不破坏挂边资格。

**验证**（`tools/test_climb.gd` 扩至 7 场景，headless 全过）：挂右/左面 jump 爬上站稳、move_up 保持挂边（入口移除）、遮挡退回挂边、LedgeSlide FULL→WallGrab、LedgeSlide jump 扒上平台、平台顶超射线范围时失败回退 LedgeSlide。

---

## 补记 6（同日追加）

### LedgeClimb 改为过程式（上爬插值）

**需求**：上爬从"瞬移式"（一帧跳到平台顶）改为"过程式"——有持续时长的爬升动作，为 ledge_climb 动画铺路。

**实现原理**：
- 进入后首帧做初始化检测（平台顶在头顶垂直射线范围内 + 头顶净空点采样），失败立即回退（头顶贴平台顶 → LedgeGrab，否则 → LedgeSlide），不启动过程
- 成功后记录 `_from_pos`（挂边位置）与 `_to_pos`（平台顶面上方，x 向平台内侧移 16px 站上台面）
- 每帧累计 `_elapsed`，`t = clamp(_elapsed / CLIMB_DURATION, 0, 1)`，位置 = `_from_pos.lerp(_to_pos, t)`（线性插值）
- 过程时长 `CLIMB_DURATION = 0.35s`（占位，动画帧确定后对齐）；过程内关重力、velocity=ZERO、不响应输入（锁定）
- `t >= 1` → 进 Idle，exit 恢复重力，当帧 move_and_slide 落定贴合平台顶

**验证**：`tools/test_climb.gd` 7 场景全过；序列显示 `LedgeGrab → LedgeClimb（21 帧）→ Idle`，确认过程式生效；失败回退仍为首帧立即回退（`LedgeClimb → LedgeGrab/LedgeSlide`）。

---

## 补记 7（同日追加）

### 空帧占位动画显示 error（开发期可视化）

**问题**：`_handle_anim()` 只对"动画名不存在"（`has_animation == false`）播放 error 占位；而 `ledge_*` / `slide` 已在 SpriteFrames 注册**空帧条目**（`frames: []`，2026-08-19 起的占位约定）——`has_animation` 返回 true → 正常"播放"0 帧动画 → 玩家隐身，缺失动画不可见。

**修复**：`_handle_anim` 判定改为 `has_animation(name) and get_frame_count(name) > 0`——空帧占位视为缺失 → 播放 error 占位（红色块，开发期可见）+ 一次性 `push_error` 警告；补帧后自动恢复正常播放。

**验证**：probe 确认 idle/wall_grab（有帧）正常、ledge_climb/ledge_grab（空帧）→ error 占位；`test_climb.gd` 7 场景回归全过（流程不受影响）。

---

## 补记 8（同日追加）

### 删除射线渲染调试代码

**需求**（用户指令）：射线渲染调试已不需要，删除。

**改动**（`player.gd`）：
- 删除 `debug_draw_rays` 导出变量、`_draw()`（三根判定射线渲染：垂直绿黄红/水平青橙）、`_draw_ray()` 辅助函数、`_process` 中的 `queue_redraw()` 调用
- 保留 `_point_inside_world()`（微型圆点检测）——已被生产逻辑 `is_head_touching_ledge()` 使用（接缝过滤），region 名从"射线调试可视化"改为"空间点检测"

**说明**：
- `_debug_ledge_status()`（l_* HUD 数据 + 接触变化 print）未删除——用户只要求删射线**渲染**；该 HUD 仍标注"临时调试，用完删"，需要时再清
- 射线可视化的方法论仍记录于 `docs/DEBUG_NOTES.md` §5（知识库，不随代码删除）

**验证**：`test_climb.gd` 7 场景 + `test_head.gd` 单元测试全过（状态机不受影响）。

---

## 补记 9（同日追加）

### 空帧动画改播 sprite_target（锚点占位）

**需求**（用户指令）：新增 `sprite_target` 占位动画（1 帧，用于确定精灵图中心锚点）。`_handle_anim` 三分支：
1. 动画**有帧** → 正常播放
2. 动画**只有空帧**（已注册、0 帧，如 ledge_* / slide）→ 播放 `sprite_target`（不报错）
3. 动画**不存在** → 播放 `error` + 一次性 `push_error()`（保持原行为）

**改动**（`player.gd`）：`_handle_anim` 拆分空帧/缺失两个分支，新增 `_play_fallback(fallback_name)` 辅助（兜底动画本身缺失/空帧则 stop）；`player.tscn` 注册 `sprite_target`（含用户手动调整的 run 速度 15→20、初始动画改 sprite_target、脚本 uid / 节点 unique_id 等编辑器自动改动）。

**验证**：probe 三分支全过（idle 正常 / ledge_climb 空帧 → sprite_target / 不存在 → error+push_error）；`test_climb.gd` 7 场景回归全过。

---

## 补记 10（收尾）

### 上爬手感微调（提交 `70ace00`）
- `CLIMB_DURATION` 0.35s → **0.2s**（爬升更快）
- `CLIMB_END_CLEARANCE` 10 → **20**（防穿透余量加大）
- 爬升终点从直接进 Idle 改为**经 Airborne 落定**（exit 恢复重力后当帧 move_and_slide 自然贴合平台顶）
- `error.png` 占位图更新（365 字节）

### 推送
- 8 个提交全部推送到远程（`0b14522..70ace00`）：LedgeClimb 系列（3c91b45/999910f/2ca5c53/644e59d/7fbcacc/96a29fa）+ 本次微调
- `main` 与 `origin/main` 同步 ✅

### 挂边系统现状（M1 1.6 功能完整）
- **LedgeGrab**（挂边，jump 上爬）/ **LedgeSlide**（滑落，FULL 回 WallGrab，jump 扒上平台）/ **LedgeClimb**（过程式上爬 0.2s，头顶空间检测，失败分流）
- 三判据（高度+法线+空间）判定，B1 已修复，`tools/` 回归套件 7+ 场景

### 下一步
1. **画挂边动画**（Aseprite）：ledge_grab / ledge_slide / ledge_climb 三组左右向动画 → 导入填进 SpriteFrames 占位 → 按 GDD 动画工作流调试同步（CLIMB_DURATION 将对齐动画帧时长）
2. 画动画时录屏（OBS Replay Buffer）= R1 素材
3. 之后：B3 → 手感验证 → M1 达标 → Release v0.0.3-alpha（先于 R1 首期视频）

---

## 补记 11：角色运动粒子特效（任务 + 思路记录）

**优先级**：P2（用户："优先级不高""具体事宜日后再说"）——安排在 M1 手感定型之后（依赖奔跑/着陆动作定型），已加入 ROADMAP 优先级表。

**特效范围**（用户原话）："这些粒子特效包括奔跑扬尘、着陆扬尘等零零碎碎的特效。"

**方案思路**（用户原话，三个候选，日后选定）：
1. "修改原本的动画精灵图，将粒子特效融合进精灵图中"
2. "单独绘制粒子特效精灵图，按条件触发"
3. "绘制粒子特效模板，使用godot着色器根据目标（瓦片，物品，实体）等渲染材质"

**记录人注（仅供参考，不替用户决策）**：
- 方案 1：零运行时开销、实现最简单，但特效与动作帧耦合（不可独立控制淡出/复用），动画帧冗余
- 方案 2：Godot 原生 Particles2D 按状态触发（run 状态播扬尘、落地时触发着陆尘），灵活可复用，是平台跳跃游戏常见做法
- 方案 3：最灵活（可对任意表面着色），开发成本最高，适合批量/环境特效（水花、碎屑）后续扩展
- 三者可组合：简单特效走 2，环境大面积特效以后走 3
