# 开发日志 2026-08-16

> 开发日志规范：每次开发会话记录一次，命名 `YYYY-MM-DD.md`（同一天多篇加 `-b`、`-c` 后缀）。
> 内容：做了什么 / 决策与理由 / 踩坑 / 下一步。路线图与 GDD 的变更请同步到对应文档。

---

## 本次内容

### 管理整理（项目结构）
- Git 仓库根从 `Game_Build/` 移至项目根，`docs/` 与 `Source_Files/`（Aseprite 源文件）纳入版本控制。
- 分支整理：`featrue/wall-climb` → `feature/wall-climb` → 合并入 `main`；实验分支归档为标签 `backup/*`。
- 建立本开发日志目录。

### 代码修复（New Age 提交，本人完成）
- 空中动画朝向改为按 `velocity.x` 判定（修复空中朝向 bug）。
- 空中贴墙按跳 → 优先墙跳（先于土狼跳判定）。
- 墙抓条件收紧：仅下落中（`velocity.y >= 0`）且按向墙面时进入 WallGrab。

### 决策
1. **滑铲 slide 归类至 M2 动作衔接**：slide 的职责是"结束移动的刹车脚本"，不属于 M1 核心移动循环。代码已完成（`slide.gd`）但未接线，随 M2 动作衔接开发（注册节点 + 触发 + 起身过渡）。
2. **放弃 wall_climb**：删除 `wall_climb.gd` 及残留引用（`can_wall_climb` 变量、注释块）；`wall_climb_left/right` 动画帧保留备用。

### 计划（下一阶段，见 ROADMAP 1.5 / 1.6）
- Wall 系列状态判定优化：重构 WallState 基类（统一计时器/墙方向检测）、多墙/墙角过渡策略。
- 新增平台边缘挂边状态机 `ledge_hang`：边缘检测 → 悬挂 → 攀上/落下，含动画。

## 下一步
1. Wall 系列判定优化（1.5）
2. 平台边缘挂边状态机（1.6）
3. 手感参数微调 + 动画同步验证（1.1 / 1.2 / 1.4）
